import { StyleSheet, View } from "react-native";

export const ScreenMensajesStyle = StyleSheet.create({
    scrollView:{
        flex:1,
    },
    viewContainer:{
        flex:1,
        alignItems:'center',
        minHeight:300,
        paddingTop:15
    }
})